# Optional: override auto-generated run name used by scripts.
CFD_SAE_RUN_NAME=

# Optional: force hardware preset globally (auto|colab|desktop|macbook).
CFD_SAE_HARDWARE=auto

# Optional: force base directory for data/checkpoints/embeddings.
# Useful for pointing local runs to external storage.
CFD_SAE_BASE_DIR=
